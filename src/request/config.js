const CONTEXTPATH = '/cooperate-case-server'

export default {
  // 接口
  COOPERATE: {
    // 登出
    LOGOUT: '/cooperate-case-server/sys/logout.do',
    MENUS: '/cooperate-case-server/sys/menus',
    USER: '/cooperate-case-server/sys/login-user',
    AUTH: '/cooperate-case-server/sys/auth',
    AUTH_REDIRECT: '/cooperate-case-server/sys/auth-redirect',
  }
}
